# ============================================================================
# Epstein Search — Environment Variables
# ============================================================================
# Copy this file: cp .env.local.example .env.local
# Then fill in the values below.
#
# All NEXT_PUBLIC_* vars are exposed to the browser.
# All others are server-side only.
# ============================================================================

# --- Required: AI -----------------------------------------------------------
# Groq API key for LLM summaries (Llama 3.3 70B)
# Get one at: https://console.groq.com
GROQ_API_KEY=""

# --- Required: Cache & Rate Limiting ----------------------------------------
# Upstash Redis — used for search result caching (24h TTL) and rate limiting
# Get credentials at: https://console.upstash.com
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# --- Required: Worker Service ------------------------------------------------
# HMAC-SHA256 shared secret between Vercel app and Render worker
# Generate with: openssl rand -hex 32
WORKER_SHARED_SECRET=""

# Worker service URL (Render deployment)
# Preferred: WORKER_URL — legacy: RENDER_WORKER_URL (both work, WORKER_URL takes priority)
WORKER_URL=""
RENDER_WORKER_URL=""

# --- Required: Database -----------------------------------------------------
# Neon Postgres connection string (consent logs + document metadata cache)
# Get at: https://console.neon.tech
NEON_DATABASE_URL=""

# --- Required: Security & Auth ----------------------------------------------
# Shared secret for cron endpoints (/api/consent/cleanup, /api/cron/refresh-docs)
# Generate with: openssl rand -base64 32
CRON_SECRET=""

# Consent policy version — increment on policy changes (semver)
NEXT_PUBLIC_CONSENT_POLICY_VERSION="1.0.0"

# --- Optional: Proxy --------------------------------------------------------
# HTTP proxy for worker to bypass Akamai IP blocking on DOJ API
# Also used by scripts/crawl-metadata.ts for local metadata seeding
# Format: http://username:password@host:port
# Free tier: https://www.webshare.io (10 IPs, 1GB/mo)
PROXY_URL=""

# --- Optional: App -----------------------------------------------------------
# Canonical base URL (defaults to Vercel deployment URL)
NEXT_PUBLIC_BASE_URL=""

# --- Optional: Monetization -------------------------------------------------
# Google AdSense publisher ID (e.g. pub-1234567890)
NEXT_PUBLIC_ADSENSE_ID=""

# Bitcoin donation address
NEXT_PUBLIC_BTC_ADDRESS=""

# Ethereum donation address
NEXT_PUBLIC_ETH_ADDRESS=""
