services:
  - type: web
    name: epstein-worker
    runtime: docker
    region: oregon
    plan: free
    branch: main
    dockerfilePath: ./worker/Dockerfile
    dockerContext: ./worker
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health
